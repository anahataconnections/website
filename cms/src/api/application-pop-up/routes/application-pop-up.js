'use strict';

/**
 * application-pop-up router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::application-pop-up.application-pop-up');
